extends base.pug

block contentMain
    include partials/contentNav.pug
    div
        h2=content.title 
        h4=content.createdAt
    div
        //- img(src="/"+loggedInUser.profileImage, width="100", height="100")
    div 
        p=content.description
    div
        a(href=`${content._id}/edit`) 수정
        a(href=`${content._id}/delete`) 삭제